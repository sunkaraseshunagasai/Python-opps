#Polymorphism
#     |
#poly-many,morphism-forms
#having many forms
#An ability can do more than one task
#operator overloading is example of polymorphism
#operator overloading-one operator performing different in different cases
#one operator can handle more than one operation
#Examples of polymorphism-->'+' it performs as add and also as concatenation
#                       |-->print(),len() .. and also perfoms more than one task
#There are two types of polymorphism-->static polymorphism(compile time polymorphism)
#                                   |-->dynamic polymorphism(run time polymorphism)
